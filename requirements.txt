# ---------------------------------------------------------------
# SRJ Training dependencies
# ---------------------------------------------------------------
# PyTorch — CPU-only build (works on any laptop).
# For GPU support replace the index URL with the appropriate
# CUDA variant from https://pytorch.org/get-started/locally/
# e.g. --index-url https://download.pytorch.org/whl/cu121
torch>=2.0
torch-geometric>=2.4

# HEP I/O
uproot>=5.0
awkward>=2.0

# Scientific Python
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
pandas>=2.0
tqdm>=4.65
networkx>=3.0

# Config parsing
pyyaml>=6.0

# Quantum layers (required for QLundNet)
pennylane>=0.35
